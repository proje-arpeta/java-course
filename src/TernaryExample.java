public class TernaryExample {
    public static void main(String[] args) {
        int number = 10;
        String result = (number >= 0) ? "Sayı pozitiftir." : "Sayı negatiftir.";
        System.out.println(result);
    }
}
